%ul.mediaList
  -@media_entries.each do |entry|
    %li.mediaEntry
      %span
        %img.mediaCover{ :src => entry[:cover], :width => "200", :height => "250" } 
      %span
        %h6= entry[:name]
          
